package pt.isel.ipw.http.errors

enum class ErrorCode {
    INTERNAL_ERROR,
    USER_ALREADY_EXISTS,
    USER_NOT_FOUND,
    USER_NOT_ACTIVE,
    INSECURE_PASSWORD,
    INVALID_CREDENTIALS,
    INVALID_ROLE,
    INVALID_ROLES,
    INVALID_TOKEN,
    REFRESH_NOT_FOUND,
    LOGIN_EXPIRED,
    ACCESS_EXPIRED,
    REFRESH_EXPIRED,
    CANNOT_DEACTIVATE_SELF,
    AREA_REQUIRED,
    AREA_NOT_FOUND,
    AREA_ALREADY_HAS_SUPERVISOR,
    INVALID_OFFSET,
    INVALID_LIMIT,
    PROCESS_NOT_FOUND,
    INVALID_PROCESS_NAME,
    INVALID_LOCATION,
    INVALID_EXPIRATION_DATE,
    INVALID_PRIORITY,
    INVALID_INVESTIGATOR,
    INVALID_SUPERVISOR,
    INVALID_TRIATOR,
    INVALID_INSURANCE,
    INVALID_TYPIFICATION,
    UPDATE_NOT_ALLOWED,
    INVALID_USER_ID,
    INVALID_AREA_ID,
    REQUEST_ERROR,
    INVALID_CONTENT,
    NOT_ASSOCIATED,
    UNAUTHORIZED_INVESTIGATOR,
    UNAUTHORIZED,
    REPORT_NOT_FOUND,
    ALREADY_REJECTED,
    NOT_APPROVED_BY_SUPERVISOR,
    ALREADY_APPROVED,
    PROVE_NOT_FOUND,
    INVALID_FILE_NAME,
    INVALID_CONTENT_TYPE,
    INVALID_FILE_SIZE,
    INVALID_STORAGE_KEY,
    STORAGE_ERROR,
    INVALID_NOTE_REQUEST,
    NOTE_NOT_FOUND,
    PROCESS_FINISHED
}
